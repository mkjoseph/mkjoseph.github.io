// this is the bubble sort
#include <iostream>


int main()
for(a=1; a<size; a++)
    for(b=size-1; b>=a; b--) {
        if (nums[b-1]) > nums[b]) { // if out of order
        // exchange elements
        t = nums[b-1];
        nums[b-1] = numbs[b]
        nums[b] = t

        }
        return 0;
    }